# Invoice-Automation
This project demonstrates a light weight but effective automation model for businesses to process their invoices. The processes are divided into multiple workflows with each having its own exception handling. This is a real world deployable automation that requires minimal configuration and setup making it easy to work with
